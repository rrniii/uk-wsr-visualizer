# Documentation source

This directory contains the Sphinx source for the UK WSR Visualizer documentation site. The site uses MyST Markdown, sphinx-design cards, and the PyData Sphinx Theme.

The main entry point is `index.md`. The Sphinx settings live in `conf.py`, and custom theme tweaks live in `_static/custom.css`.
